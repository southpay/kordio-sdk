name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.8
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
      - name: Trusted publishing needs npm 11.5.1 or later
        run: |
          npm install -g npm@latest
          version="$(npm --version)"
          echo "npm $version"
          major="${version%%.*}"
          if [ "$major" -lt 11 ]; then
            echo "npm $version is too old for OIDC trusted publishing; need 11.5.1+" >&2
            exit 1
          fi
      - run: bun install --frozen-lockfile
      - run: bun run verify
      - name: Every documented path is served by the live API
        run: bun run check:routes
      - name: Tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          tag="${GITHUB_REF_NAME#v}"
          pkg="$(node -p "require('./package.json').version")"
          if [ "$tag" != "$pkg" ]; then
            echo "tag $tag does not match package version $pkg" >&2
            exit 1
          fi
      - name: Publish via npm trusted publishing (OIDC, no stored credential)
        run: npm publish --provenance --access public
